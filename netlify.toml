[build]
  command = "npm run build"
  publish = "build"
  functions = "netlify/functions"

# Redirect per SPA (Single Page Application)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Headers per sicurezza
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"

# Headers per file statici
[[headers]]
  for = "/static/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# Variabili d'ambiente per produzione
[build.environment]
  NODE_VERSION = "18"
  NPM_VERSION = "9"
  SECRETS_SCAN_ENABLED = "true"
  SECRETS_SCAN_OMIT_PATHS = "README*.md,DOCUMENTAZIONE*.md,NETLIFY*.md,.env.example,.eslintignore,build/static/js/*.js,build/static/js/*.LICENSE.txt"
  SECRETS_SCAN_OMIT_KEYS = "REACT_APP_API_URL,REACT_APP_ENV"